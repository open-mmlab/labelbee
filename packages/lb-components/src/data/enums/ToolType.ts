import { cTool } from '@labelbee/lb-annotation';

const { ETextType, EToolName, EScribblePattern } = cTool;

export { ETextType, EToolName, EScribblePattern };

/** 文本标注字数上限 */
export const TEXT_ATTRIBUTE_MAX_LENGTH = 1000;
