import {
  backward,
  dargWithLeftClick,
  dargWithRightClick,
  forward,
  fullScreen,
  rotate,
  saveResult,
  scale,
  setValid,
} from '../common';

const textToolShortCutTable = [
  saveResult,
  scale,
  rotate,
  fullScreen,
  setValid,
  backward,
  forward,
  dargWithRightClick,
  dargWithLeftClick,
];

export default textToolShortCutTable;
